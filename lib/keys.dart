import 'package:flutter/material.dart';

class ConverterPageKeys {
  static const _pageName = 'ConverterPage';

  static const Key inputField = Key('${_pageName}_inputField');
  static const Key convertButton = Key('${_pageName}_convertButton');
  static const Key outputField = Key('${_pageName}_outputField');
}
